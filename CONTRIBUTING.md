# Contributing

Use short-lived branches named `feature/<description>`, `fix/<description>`, or `chore/<description>`. Merge through pull requests after tests and formatting pass; do not commit directly to `main`.

Use Conventional Commit-style messages, for example `feat(payment): add idempotency record` or `fix(ledger): reject unbalanced entry`. Keep commits focused and include tests with behavioral changes.
